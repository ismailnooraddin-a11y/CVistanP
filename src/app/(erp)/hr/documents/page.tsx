import PageShell from '@/components/page-shell';import {requireMembership} from '@/lib/auth';
export default async function Page(){const {supabase,membership}=await requireMembership();const {data}=await supabase.from('employee_documents').select('*,employees(first_name,last_name)').eq('branch_id',membership.branch_id);return <PageShell title="Employee Documents" subtitle="Documents assigned to employee records"><div className="notice">Create a private Supabase Storage bucket named <strong>employee-documents</strong>. Document metadata is protected by branch-level RLS.</div><div style={{height:16}}/><div className="table-wrap"><table><thead><tr><th>Employee</th><th>Document</th><th>Type</th><th>Expiry</th></tr></thead><tbody>{(data||[]).map((r:any)=><tr key={r.id}><td>{r.employees?.first_name} {r.employees?.last_name}</td><td>{r.title}</td><td>{r.document_type}</td><td>{r.expiry_date||'—'}</td></tr>)}</tbody></table></div></PageShell>}
